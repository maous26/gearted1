services:
  - type: static_site
    name: gearted-mobile-app
    buildCommand: echo "Using pre-built Flutter web files with grey screen fix"
    staticPublishPath: ./web-build
    envVars:
      - key: FLUTTER_WEB
        value: "true"
    domains:
      - www.gearted.eu
      - gearted.eu
